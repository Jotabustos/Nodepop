"use strict";

function checkTagFilter(tag) {
    let tagToFilter;
    if(tag.includes(",")){ // Several tags requested
        let tagArray = tag.split(",");
        tagToFilter = {}

    }
}
module.exports = checkTagFilter;
